#include <iostream>
using namespace std;
int main()
{
    int n;
    cout << "Enter the size of the array:";
    cin >> n;

    int array[n];
    for (int i = 0; i < array[n]; i++)
    {
        cin>>array[i];
    }
    for (int i = 0; i < array[n]; i++)
    {
        cout<<array[i];
    }
    int key;
    cout<<"Enter the key you want to search for"
    
    
    
}

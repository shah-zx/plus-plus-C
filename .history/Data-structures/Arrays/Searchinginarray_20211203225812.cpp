#include <iostream>
using namespace std;
int linearSearch(int x, int arr[] , int ) {
    for (int i = 0; i < count; i++)
    {
        /* code */
    }
    
}


int main()
{
    int n;
    cout << "Enter the size of the array:";
    cin >> n;

    int array[n];
    for (int i = 0; i < array[n]; i++)
    {
        cin>>array[i];
    }
    for (int i = 0; i < array[n]; i++)
    {
        cout<<array[i];
    }
    int key;
    cout<<"Enter the key you want to search for:";
    cin>>key;
    
    
    
}

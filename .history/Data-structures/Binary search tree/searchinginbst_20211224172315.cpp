#include <bits/stdc++.h>
#include <stack>
#include <queue>
#include <algorithm>
using namespace std;

Node *BST(Node *root, int k)
{
    if (root == NULL)
    {
        return NULL;
    }
    if (root->data == k)
    {
        return
    }

    if (val < root->data)
    {
        return BST(root->left, val)
    }
    else
    {
        return BST(root->right, val)
    }
}

int main()
{

}
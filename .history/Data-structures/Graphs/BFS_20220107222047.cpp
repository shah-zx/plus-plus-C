#include <bits/stdc++.h>
#include <stack>
#include <queue>
#include <math.h>
#include <algorithm>
using namespace std;
#define vi vector<int>
#define vii vector<pair<int, int>>
#define pii pair<int, int>
#define rep(i, a, b) for (int i = a; i < b; i++)

vector<int> BFSofgraph( vector<int> adj[] , int V , int s)  // Nodes and adjacency list 
{
   vector <bool> vis(V + 1, false);
   queue<int> q;
   vis[s] = true; //
   q.push(s);
   



}

signed main()
{

// creation of an adjency list :

int V;
vector <int> adj[V];


}

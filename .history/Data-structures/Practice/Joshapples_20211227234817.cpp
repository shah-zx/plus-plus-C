#include <bits/stdc++.h>
#include <stack>
#include <queue>
#include <algorithm>
using namespace std;
int main()
{
    // int n, M1, P1, M2, P2;
    // cin >> n >> M1 >> P1 >> M2 >> P2;

    // if (n % M1 == 0)
    // {
    //     cout << (n / M1) * P1;
    // }
    // else if (n % M2 == 0)
    // {
    //     cout << (n / M2) * P2;
    // }
    int n;
    cin>> n;
    for (int i = 0; i < n; i++)
    {
        int rem = 0;
        while (n%3!=1)
        {
            n
            int ld = n%10;
            rem += 
            n = n/10;
        }
        
    }
    

}
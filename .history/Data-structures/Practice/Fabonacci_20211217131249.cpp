#include <iostream>
using namespace std;

int Fib(int n)
{
    int ret = 0;
    for (int i = 0; i < n; i++)
    {
        return = Fib(n - 2) + Fib(n - 1) ;
    }
}

int main()
{
    cout << Fib(5);
}
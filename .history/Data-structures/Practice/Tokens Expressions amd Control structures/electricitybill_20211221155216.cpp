#include <bits/stdc++.h>
#include <stack>
#include <queue>
using namespace std;

int main()
{
    string s[4];
    for (int i = 0; i < s.length(); i++)
    {
        cin >> s[i];
    }
    float n;
    cout << "Enter the number of units consumed";
    cin>>n;
    if (n<=100 && n>=1)
    {
        /* code */
    }
    
}
#include<bits/stdc++.h>
#include<stack>
#include<queue>
#include<algorithm>
using namespace std;
int main(){
    char arr[15] = {};
    for (int i = 0; i < 5; i++)
    {
        if (arr[i] == 'a' || arr[i] == 'e' || arr[i] == 'o' || arr[i] == 'u' || arr[i] == 'y')
        {
            /* code */
        }
        
    }
    
}
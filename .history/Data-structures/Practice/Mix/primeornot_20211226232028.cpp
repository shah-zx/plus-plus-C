#include <bits/stdc++.h>
#include <stack>
#include <queue>
#include <algorithm>
#include<math.h>
using namespace std;

bool negativeorPositive(int n)
{
    if (n >= 0)
    {
        return true;
    }
    else
    {
        return false;
    }
}
void isPrime(int n)
{
    if(negativeorPositive)
    {
        for (int i = 0; i < ; i++)
        {
            /* code */
        }
        
    }
}

int main()
{
    int n;
    cin >> n;
    negativeorPositive(n);
}
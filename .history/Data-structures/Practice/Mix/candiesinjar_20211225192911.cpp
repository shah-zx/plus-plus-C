#include <bits/stdc++.h>
#include <stack>
#include <queue>
#include <algorithm>
using namespace std;

int main()
{
    int total = 10;
    int k = 5;
    int n;
    cin >> n;
    if (n > total)
    {
        cout << "INVALID INPUT";
    }
    int remain = total - n;
    if (n <= k)
    {
        cout << "NUMBER OF CANDIES SOLD : " << n << endl;
        cout << "NUMBER OF CANDIES AVAILABLE :" << left;
    }
}
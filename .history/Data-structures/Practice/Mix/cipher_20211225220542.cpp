#include <bits/stdc++.h>
#include <stack>
#include <queue>
#include <algorithm>
using namespace std;

string Ciser(string s, int n)
{
    for (int i = 0; i < s.size(); i++)
    {
        int abhi = s[i] - '0';
        abhi + n;
    }
    cout << s;
}

int main()
{
    char arr[100] = {};
    int n;
    cin >> n;
    // cout << Ciser(s, n);
}
#include <bits/stdc++.h>
#include <stack>
#include <queue>
#include <algorithm>
using namespace std;

class Node
{
    int data;
    Node *next;
    Node(int val)
    {
        data = val;
        next = NULL;
    }
};

void Insert( Node *head , int val)
{
    if (head == NULL)
    {
        /* code */
    }
    
}

int main()
{
}
#include <bits/stdc++.h>
#include <stack>
#include <queue>
#include <algorithm>
#include <string>
using namespace std;
int main()
{
    string s;
    cin >> s;
    char c;
    cin >> c;
    string e;

    if (c == 'y' || c == 'Y')
    {
        getline(cin, s);
    }
    else
        return 0;
}
#include<iostream>
using namespace std;


string Finddup(string s)
{
    if (s.length()==0)
    {
        return;
    }
    if (s[0]==s[1])
    {
        
    }
    string ros = Finddup(s.substr(1));

    
}

int main(){

}
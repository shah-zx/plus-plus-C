#include <bits/stdc++.h>
#include <stack>
#include <queue>
#include <math.h>
#include <algorithm>
using namespace std;
#define vi vector<int>
#define rep(i, a, b) for (int i = a; i < b; i++)
int_fast32_t main()
{
    int m;
    cin >> m;
    int n;
    cin >> n;
    int arr[m][n];
    for (int i = 0; i < m; i++)
    {
        for (int j = i; j <= n; i++)
        {
            cout << arr[m][n] << " ";
        }
        cout << endl;
    }
}
#include <bits/stdc++.h>
#include <stack>
#include <queue>
#include <algorithm>
using namespace std;
#define vi vector<int>
#define rep(i, a, b) for (int i = a; i < b; i++)
#define repm(i, a, b) for (int i = a; i > b; i--)
#define ai int arr[]

int_fast32_t main()
{

    int n;
    cin >> n;
    int arr[n];
    rep(i, 0, n)
    {
        cin >> arr[i];
    }
    repm(i, n, 0)
    {

    }
}
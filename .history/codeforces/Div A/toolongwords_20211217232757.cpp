#include <bits/stdc++.h>
using namespace std;

int main()
{
    int num = num;
    cin >> num;
    string s;
    for (int i = 0; i <= num; i++)
    {
        cin >> s;
    }
    int n = s.length() - 2;
    for (int i = 0; i <= num; i++)
    {
        if (s.length() > 10)
        {
            cout << s[0] << n << s[s.length() - 1];
        }
        else
        {
            cout << s;
        }
    }
}
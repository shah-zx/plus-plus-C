#include <bits/stdc++.h>
#include<string>
using namespace std;

int main()
{
    int num = num;
    cin >> num;
    string s[num];
    stoi(s);
    for (int i = 0; i < s.size(); i++)
    {
        
    }
    
}

// cout << s[0] << s[i].length() - 2 << s[i].length() - 1;

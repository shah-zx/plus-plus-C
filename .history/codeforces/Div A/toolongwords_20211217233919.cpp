#include <bits/stdc++.h>
using namespace std;

int main()
{
    int num = num;
    cin >> num;
    string arr[num] = {};
    for (int i = 1; i <= num; i++)
    {
        cin >> arr[i];
    }
    for (int i = 1; i <= num; i++)
    {
        if (arr[i].length() >= 10)
        {
        }
        else
        {
            cout << s;
        }
    }
}


            // cout << s[0] << s[i].length() - 2 << s[i].length() - 1;

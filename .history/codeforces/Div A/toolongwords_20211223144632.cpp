#include <bits/stdc++.h>
using namespace std;

int main()
{
    int num = num;
    cin >> num;
    string s[num];
    for (int i = 1; i <= num; i++)
    {
        cin >> s[i];
    }
    for (int i = 1; i <= s[i].size(); i++)
    {
        if (s[i].length() >= 10)
        {
        }
        else
        {
            cout << s;
        }
    }
}

            // cout << s[0] << s[i].length() - 2 << s[i].length() - 1;

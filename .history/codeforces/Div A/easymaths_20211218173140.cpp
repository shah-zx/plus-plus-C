#include<bits/stdc++.h>
using namespace std;





int main(){
   string s;
   cin>>s;
   char c1 = '1';
   char c2 = '2';
   char c3 = '3';

   for (int i = 0; i < s.size(); i++)
   {
       if (s[i] == c1 || s[i] == c2 || s[i] == c3)
       {
           cout<<"Got";
       }
           
   }
   retrun s;
   
}